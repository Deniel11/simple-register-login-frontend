export class User {
    id!: Number;
    username!: String;
    email!: String;
    dateOfBirth!: String;
    admin!: Boolean;
    verified!: Boolean;
    enabled!: Boolean;

    constructor() {
    }
}
